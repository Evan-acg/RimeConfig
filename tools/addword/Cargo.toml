[package]
name = "adw"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
which = "7"
colored = "2"

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = "z"
